# Example 05: calibrate on a record with missing air temperature, after filling the gaps by interpolation.
# The input file is made by run.py. From the repository's top folder:
#   pyair2stream --config examples/05_gaps/filled.yaml
project_name: "gaps"
station_name: "Mentue"
series: "c"
time_resolution: "1d"
version: 8
integrator: "CRN"
objective_function: "NSE"
run_mode: "DE"
random_seed: 42

paths:
  input_data: "examples/05_gaps/output/filled_calibration.csv"
  validation_data: "data/switzerland/MAH_2369_validation.csv"
  output_dir: "examples/05_gaps/output/filled"

parameter_bounds:
  min: [-5, -5, -5, -1, 0,  0,  0, -1]
  max: [15, 1.5, 5,  1, 20, 10, 1,  5]
