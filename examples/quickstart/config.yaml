# Minimal quick-start example: synthetic "River Alpha" daily data.
# Run from the repository root with:
#   pyair2stream --config examples/quickstart/config.yaml
project_name: "quickstart"
station_name: "River_Alpha"
series: "c"
time_resolution: "1d"
version: 8
integrator: "CRN"          # recommended (unconditionally stable)
objective_function: "NSE"
run_mode: "DE"             # recommended calibration method
random_seed: 42            # makes the calibration exactly repeatable

paths:
  input_data: "examples/quickstart/data/calibration_data.csv"
  validation_data: "examples/quickstart/data/validation_data.csv"
  output_dir: "examples/quickstart/output"

optimization:
  n_run: 50                # maximum DE generations
  n_particles: 5           # DE population = 5 x 8 parameters = 40 candidates

parameter_bounds:          # ranges used by the original model's authors
  min: [-5, -5, -5, -1, 0,  0,  0, -1]
  max: [15, 1.5, 5,  1, 20, 10, 1,  5]
